import { useState } from "react"

export const NotFound = () => {

    return (
        <>
        <div className="h-screen w-screen flex items-center justify-center text-white border border-red-500">
            {/* <button onClick={()=>{setCount(count+1)}}>Increase</button>
            <button onClick={()=>{setCount(count-1)}}>Increase</button> */}
            404 NotFound
        </div>
        </>
    )
}
